server:
  port: 9296

spring:
  profiles:
    active: dev
  application:
     name:  CONFIG-SERVER
  cloud:
     config:
       server:
          git:
            uri: https://github.com/jbfm21/cloud-config-server
            default-label: main
            clone-on-start: true